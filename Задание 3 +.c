/******************************************************************************

                            Online C Compiler.
                Code, Compile, Run and Debug C program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/

#include <stdio.h>

int main()
{
    int a,b,c;
    printf("Введите значения\n");
    scanf("%d",&a,&b,&c);
    int gc,numb,numa;
    numa=a*a;
    gc=c*c;
    numb=b*b;
    if (gc=numa+numb);
       printf("abc прямоугольный\n");
      else   printf("abc непрямоугольный\n");
      if (gc<numa+numb);
        printf("abc остроуольный\n");
     else